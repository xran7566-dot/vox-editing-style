---
name: vox-editing-style
description: 制作或审核保留原声、原表演和博主人设的真人口播 Vox 拼贴短视频。用于原始视频/SRT/文稿/公开链接拆解、生成制作审核脚本与稳定性清单、抖音/竖屏或横屏口播剪辑、Vox 纸媒拼贴、Remotion 关键帧审核、中文字幕可读性、按 SRT/原声对齐进度和时间线动画、转场/节点/结论音效、动态真人窗口/抠像、语义视觉翻译、MG 动画、平台安全区和局部返工；当用户提到“Vox剪辑风格”“原始资料拆解”“审核脚本”“稳定生成”“字幕太小”“进度没对齐”“加音效”“Director 融合”或“真人口播加 MG”时使用。
---

# Vox剪辑风格

用 Vox 作为主视觉系统，用 Director 的主题、组件、版式、关键帧和运动能力作为视觉引擎；用 Remotion 保留原声、原表演、动态真人、字幕、安全区和可局部返工的时间轴。MG 是增强层，不替代 Vox 的画面主导权。

先读 [完整运行层](references/fusion-operating-model.md)。完整交付路径见 [端到端流程图](references/vox-editing-workflow.md)。处理任何原始资料时必读 [VOX-CONTENT 内容编导层](references/vox-content.md) 和 [原始资料审核脚本合约](references/source-audit-contract.md)；进入执行阶段时必读 [剪辑执行大纲合约](references/edit-execution-outline.md)；需要确定视觉路由时读 [Director 集成](references/director-integration.md)；需要制作动画时必读 [分层场景合约](references/layered-scene-contract.md) 和 [MG 任务单](references/mg-task-card.md)；制作字幕、时间线/进度动画或音效时必读 [字幕·动画·声音规范](references/subtitle-motion-sound.md)；提交前读 [审批与验收](references/approval-and-qc.md)。针对已经反复出现的裁切、遮字幕、侧边文字化、时间段遗漏和套用旧版式，额外执行 [反复问题防错闸门](references/recurring-failure-gates.md)。侧边元素按整帧视觉判断，不是固定的“只能图像”规则。
进入视觉制作前还必须执行 [Director 主生成路径](references/director-first-route.md)：Director 负责主拼贴生图，Remotion 只负责合成，SVG 只能辅助；Director 资产缺失时必须阻断，不得降级。

## 不可改变的优先级

1. 保留原始视频、原声、口型、表演、人设和文案语义；原声/SRT 是时间主轴，禁止重配音。
2. 先定义观众要理解、相信、感受或行动什么；按语义变化切段，不按标点硬切，也不把一句话固定成一镜。
3. Director 决定主题、视觉隐喻、组件、构图、色彩、字体、材质和镜头/元素运动；外层只提供语义、参考范围、真人目的、安全区和禁令。
4. 每段先完成可读的编辑型关键帧，再设计动画；关键帧必须是实际 Remotion 合成截帧，不能交付孤立生图或素材板。
5. 已批准的某张画面只在标记范围内使用。`scene_solution` 只服务该语义段；不得把一张认可图、一个颜色、一个版式或一类组件推断为全片模板。

## 每次制作的流程

### 1. 审计输入与建立运行单

收集：原始口播视频、SRT/时间字幕、纯文本、参考视频/图、平台画幅、时长、受众和传播目标。

**画幅必须先确认（硬闸门）**：开始内容拆解或任何视觉生成前，必须向用户明确确认最终成品画幅（例如 9:16 竖屏、16:9 横屏）以及是否保持原片比例。不得根据历史项目、参考片或素材内容自行猜测、改横屏/改竖屏或套用旧版式。用户未确认时只能做资料审计，禁止生成关键帧和渲染；若用户要求保持原版，就锁定原视频画幅，后续所有人物窗口、拼贴资产、安全区和字幕参数都按该画幅重构。

先执行 `VOX-CONTENT` 内容拆解，生成可追溯的编导提纲；再进行原始资料审核并写运行单。没有完成内容拆解，不得直接进入视觉制作：

- 输入为用户提供的公开抖音/小红书链接，或可免费访问的视频号链接时，如果当前环境已安装兼容的拆解 Skill，可调用 `$zhiqi-ip-talking-head-decomposer` 执行“提取+拆解”。该 Skill 是可选适配器，不是本 Skill 的硬依赖；如不存在，就地使用用户提供的视频、SRT 或文稿完成审计。默认只提取文案和必要的结构信息；用户未明确要求时不下载原视频、不调用付费 API。
- 输入为本地视频、SRT、文稿、图片或录屏时，直接本地拆解；不为了调用拆解 Skill 而上传私有素材。
- 输入混合时，将公开链接的提取结果与本地原片/SRT 合并，但每条结论必须保留来源身份。

拆解后生成两个文件：`source-review.md` 供用户阅读审批，`source-audit.json` 供程序校验。审核脚本必须锁定素材 SHA-256、创作者身份、原文/时间段、主张来源、语义单元、必须保留、禁止虚构、参考范围、素材缺口、未解决问题和声音方案。 BGM 情绪、能量曲线和音效节点必须在这一步根据文案语意锁定，后续不得自由发挥。
优先从 `assets/source-review-template.md` 和 `assets/source-audit-template.json` 复制工作文件，填写后用 `scripts/fingerprint_source_audit.py` 生成带指纹的版本。

先交付 `source-review.md` 让用户审批。未批准时将 `approval.status` 保持为 `draft`；只有用户确认后才能设为 `approved`。运行：

```bash
python3 scripts/validate_source_audit.py /absolute/path/to/source-audit.json
```

审核脚本未通过、素材指纹变化、存在 `blocking` 项或用户未批准时，禁止进入 fusion run、关键帧或 Remotion。

没有明确说“不要 BGM”时，默认生成一条低调、纯器乐、可移除的本地 BGM，并在 fusion input 中使用 `music.mode=local_track` 和轨道路径。可使用随 Skill 发布的标准库脚本生成：

```bash
python3 scripts/generate_local_bgm.py --duration 105 --mood focused --output /absolute/path/to/generated-bgm.wav
```

只有用户明确要求不加 BGM 时才设为 `music.mode=disabled`。需要使用付费音乐生成时，仍需单独披露成本并获得确认。

通过审核后，根据 `source-audit.json` 为每个语义单元写 `director_brief`：观众结果、必须看见的关系、原声语境、参考范围、必须保留和必须避免。同步生成 `edit-outline.md` 剪辑执行大纲，逐段列出时间、原声/字幕、剪辑目的、真人状态、画面与动效、字幕/进度、BGM/音效和返工规则。不得提前替 Director 规定具体隐喻、组件、配色、构图或真人形式。

每单元再写 `persona_brief`：真人只为人设记忆服务；允许的动态形式为全屏、动态抠像、圆形/方形/矩形动态窗口或不出现。不能指定开场必须露脸或固定秒数露脸。

运行：

```bash
python3 scripts/verify_director_vendor.py
python3 scripts/validate_source_audit.py /absolute/path/to/source-audit.json
python3 scripts/create_fusion_run.py --input /absolute/path/to/input.json --run-dir /absolute/path/to/fusion-run
python3 scripts/validate_fusion_run.py /absolute/path/to/fusion-run/fusion-run.json
```

运行单未通过，不进入关键帧与 Remotion。

### 2. 视觉提案与关键帧

Director 按语义提出画面，不把旧的 Remotion 镜头直接重新截出来当“新生成”。每个语义单元只保留一个绝对视觉中心，但可用照片、物件、纸条、标签、路线、地图、卡片、面板、受控 3D、MJ/MG 等多个从属组件共同解释关系。

完成视觉提案后，必须先用实际 Remotion 合成生成预览，再按语义节点抽取关键帧或短片段供编导审核；关键帧审核通过后才能进行完整时间轴渲染。预览抽帧不是最终 QC 的替代品，最终成片仍需执行完整画面、音频和时序验收。

视觉丰富度不是把少数几张图反复摆上去：执行大纲必须按语义节点规划真实视觉资产、主次图层、纸媒/照片/贴图、MG 动画、空间关系和接力变化。每个节点要写清新增信息和运动机制；资产或层次不足时必须先补足或报告缺口，不得用重复图片、空白卡片或抽象 SVG 凑时长。

**分层场景是渲染硬闸门**：每个进入 Remotion 的语义镜头必须先生成 `layer-manifest.json`，并通过 `scripts/validate_layer_manifest.py`。背景、结构/中景、主物件/信息、前景/连接关系中至少存在 3 个可独立控制的视觉层；真人、字幕、渐变、调色和音频不计入这 3 层。同一张完整拼贴图的复制、裁切、模糊、放大或遮罩只能算 1 个源，不能冒充多个图层。已经包含全部主物件和关系的压平海报只能作为参考或底纹，不能登记为可拆分场景。每层必须记录独立来源、语义职责、进入、关系建立、落定和退出/接力；复杂关系镜头还必须存在 MG 任务单。缺少清单、素材或任务单时，禁止关键帧、Studio 和渲染。

在项目预检和渲染命令之前运行：

```bash
python3 /absolute/path/to/vox-editing-style/scripts/validate_layer_manifest.py \
  /absolute/path/to/project/production/layer-manifest.json \
  --project-root /absolute/path/to/project
```

完整关键帧必须同时有：

- 一个可停住看的主物件/主隐喻；
- 主结论文字；
- 1–2 组服务关系的辅助文字、标签、纸条或印章；
- 正确的中文字幕空间；
- 若真人出现，真实动态视频/动态抠像容器、脸部净空与正确层级。

不要把“避免生图乱码”误解为“画面不要文字”。允许用无文字拼贴底图作为素材；中文标题、标签和关系说明由 Remotion 精确叠加，成为画面的一部分。

参考范围必须记录：

- `scene_solution`：只复用指定语义段的已批准方案；
- `visual_quality`：只取材质、层级、完成度；
- `technical_mechanism`：只取动态抠像、路径生长、分层组装等能力；
- `system_anchor`：仅当用户明确指定时才全片生效。

### 3. 真人与安全区

真人不是常驻主角，出现与否和形式由当前画面服务。一个画面里只出现一个真人实例和一种形式，但不得把“卡片”设为默认。根据语义和构图，可使用自然动态抠像、圆形窗口、圆角矩形窗口、局部出场或短暂隐藏；只有用户明确要求时才使用卡片式框。不得用静态 PNG 冒充说话中的真人；必须用原片动态视频或真实动态抠像。任何文字、图标、路径、标签和平台 UI 都不能遮脸。

每个镜头还必须明确选择基础画面模式：`source_video_base_overlay`（原片动态视频铺满画布，上层叠加多个 Vox/MG 独立组件）、`collage_main_with_presenter`（拼贴主画面加真人窗口）、`dynamic_cutout_fusion`（真人动态抠像与拼贴融合）或 `mixed_by_scene`（按镜头切换）。原片底片叠层是正式能力，不得误做成“把原片当背景后只加字幕”；至少需要 3 个可独立控制、按语义进出的上层视觉组件。基础模式必须由当前原片、语义和构图决定，不能继承上一项目的默认版式。

安全区按三级判断：

- 一级：脸、钩子、核心结论、关键动作和必要数字，全程保护；
- 二级：次级标签、城市/职业/兴趣端点，读清后可被轻微遮挡；
- 三级：纹理、地图边缘、装饰与非必要路径，可进入平台交互区。

默认竖屏时避开右侧约 180px、顶部约 120px、底部约 320px，以及字幕上方约 60px 的一级内容。不要为了几何安全框把优秀构图压扁；横竖版需要重新构图。允许与圆形/矩形窗口、动作方向和留白匹配的自然裁切，但禁止机械地把人物裁成半截或留下失衡空洞。

### 4. 动画与声音

在关键帧批准后，按 `进入 → 构建 → 结论 → 停留 → 接力` 制作。每段关系复杂时写 MG 任务单；缩放、拉远、淡入仅可辅助，不能充当全部动画。每个结论必须留阅读停顿：简单 0.8–1.2 秒，信息密集约 1.2–1.8 秒，随口播调整。

所有进度条、时间线、路径生长、节点点亮和步骤切换，必须由原声/SRT 中的语义事件帧驱动。先写“口播词 → 时间码 → 动画开始/落定帧”对齐表，再编码；禁止为了方便而从场景第 0 帧匀速跑到结尾。

字幕先按成片尺寸设定可读性基线，再用真实长句测试。`1920×1080` 横屏中文口播字幕默认从 `54px` 起评，不得未经视觉验收直接使用 `39px` 这类偏小字号。每个画面默认只显示一句、一行字幕；先按原声的自然停顿拆成前后两个单行字幕。只有同一句无法在安全宽度内完整显示，且拆开会破坏句意或口播节奏时，才允许该句换成两行。两行必须属于同一句、同一时间段，不得上下同时堆叠两句不同字幕。其他画幅按 [字幕·动画·声音规范](references/subtitle-motion-sound.md) 调整并截帧验收。

保留一个连续原声音频实例，不在转场重新加载。BGM 默认启用：使用独立、可移除的本地纯器乐轨，并压在人声之下；只有用户明确说“不要 BGM”或“只保留原声”时才关闭。音效仍是独立的编辑层：只服务节点点亮、关系建立、步骤切换、结论落版和 CTA，必须与视觉事件对齐，单独可关闭，并保持明显低于人声；不得每个元素都配音效。需要使用付费音乐生成服务时，必须单独披露成本并获得用户确认；绝不生成或替换配音。

### 5. 审批、成片与返工

先交 Remotion 合成关键帧，再交动画样片；渲染前逐帧检查脸部、文字、字幕、安全区、视觉中心、语义关系、阅读停留和相邻画面的差异。每次交付必须同时保留可编辑的 Remotion 工程、素材目录、时间码配置和可启动的预览入口；不能只交不可调的 MP4。预览窗口应可再次打开，让用户自行调整画幅、人物窗口、图层、字幕和节点时间。

若某段失败，按 `preserve → remove → rebuild → recheck` 只返工该段。保留基线、已批准帧、已批准片段、可编辑 Remotion 工程和版本化输出；不得为修一个漏点推翻整片。

## 明确禁止

- 不要把单张认可图变成全片排版模板。
- 不要把 Director 降级成少数纸片、色块、卡片和箭头的低配模仿。
- 不要连续重复同一人物小人、资料卡、背景或关系图。
- 不要强制前三秒或固定秒数出现真人。
- 不要把多个真人形式堆叠在同一画面。
- 不要用静态抠图替代说话中的动态真人。
- 不要让文字、图标、路线、标题和字幕挡住脸或关键结论。
- 不要在一个画面中同时显示两句不同字幕，也不要把两行当成常规排版。只有同一句不适合拆分时才能换成两行；不得用过小字号硬塞成单行。
- 不要用任意固定帧或全段匀速进度冒充语义同步；不要让节点先于对应口播点亮。
- 不要用音效遮住原声、为每个元素堆音效，或让音效与画面动作错帧。
- 不要把无文字生图素材当成最终审批帧；必须加回完整文字信息层、动态真人计划和字幕空间后再截帧审核。
- 不要把一张已经压平的完整拼贴图复制、裁切、放大或加遮罩后命名成“背景层、前景层”；重复源只能算一个图层，未通过分层清单校验不得进入 Remotion。
- 不要擅自添加 Skill、工具、模型、内部项目或 Vox/Remotion 品牌水印；默认全片禁止内部标识，只有用户明确指定时才加入。
- 不要私自调用 Atlas、TTS、图生视频或付费视觉 API；当前执行路线是本地 Remotion。素材不足时先报告缺口。
